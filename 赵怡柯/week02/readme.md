# 作业1：修改09_深度学习文本分类
- 修改了`SimpleClassifier`类的一些逻辑
- 增加了`train_loss`函数，封装训练过程，并记录损失值
- 在`train_loss`方法中增加了保存模型权重的部分，方便预测时调用
- 修改了`classify_text`函数中的一点点逻辑，使预测时能够调用正确的训练好的模型
- 自定义了对比的模型配置


# 作业2：修改06_torch线性回归.py
- 修改了数据生成部分
- 把训练部分修改为了双层for循环（增加了batch）
- 构建了两层隐藏层的全连接网络
- 绘制损失图像和拟合后的曲线与原曲线的对比